{
  "name": "better-songify",
  "version": "0.1.0",
  "private": true,
  "description": "Buildable BetterSongify extension for Spotify UI improvements: lyric translation, transliteration, video preferences, and more.",
  "type": "commonjs",
  "scripts": {
    "build": "node tools/build.js",
    "check": "node --check src/index.js && node --check src/chrome/background.js && npm run build && node --check dist/BetterSongify.js && node --check dist/chrome/content.js && node --check dist/chrome/background.js",
    "install:macos": "bash install-macos.sh install",
    "reinstall:macos": "bash install-macos.sh reinstall",
    "remove:macos": "bash install-macos.sh remove"
  }
}
